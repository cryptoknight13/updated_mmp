(define (domain domaingotocity)
(:requirements :typing)
(:types city - object)
(:predicates 
    (at ?x - city)
    (has_taxi ?x ?y - city)
    (has_bus ?x ?y - city)
    (neighboring ?x ?y - city)
)
  
%OPERATORS%


)
